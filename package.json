{
  "devDependencies": {
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "axi": "^1.0.0",
    "axios": "^0.20.0",
    "json-server": "^0.16.1"
  },
  "scripts": {
    "start:db": "json-server --watch db.json",
    "start:parcel": "parcel index.html"
  }
}
